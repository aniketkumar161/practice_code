student = {"name": "Aniket", "cgpa": 8.5}

student.update({"marks": 420})   # add/update
print(student.get("name"))       # get value
print(student.keys())            # keys
print(student.values())          # values
print(student.items())           # key-value pairs
